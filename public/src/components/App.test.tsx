import { render, screen, fireEvent } from '@testing-library/react'
import App from './App'

// Remove this line: jest.mock('./App')

describe('App Component', () => {
  // ... rest of your tests remain the same
})
